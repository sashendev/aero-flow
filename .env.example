# Copy this to `.env` and fill in values for local development.
# Never commit your real `.env` file.

# AirLabs API key (required for flights & route lookups)
AIRLABS_API_KEY=

# Optional: override the AirLabs API base URL (rarely needed)
AIRLABS_BASE=https://airlabs.co/api/v9
